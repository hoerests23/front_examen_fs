# Configuración de Proxy definida en vite.config.ts

# El frontend corre en el puerto por defecto de Vite
FRONTEND_PORT=5173

# Redirección de peticiones API
# Todas las peticiones a '/api' se redirigen al backend
VITE_API_PROXY_TARGET=http://localhost:8080

# Nota:
# Asegúrese de que el backend esté corriendo en el puerto 8080 
# para que el proxy de Vite funcione correctamente.